## Styling

> [!important]
>
> p5.js theme color - #ed225d

React Native provides its styling API but I am more familiar with Tailwind so we'll use the NativeWind package to make this possible.

The design of the app will extend the existing design system currently in use on the new p5.js website by porting it.
