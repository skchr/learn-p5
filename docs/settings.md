## Settings

There's really not much for users to configure.

- Daily reminders
- Theme
- Font size
- Text wrapping
- Export app data
- Reset progress

These settings are saved in the `settings.json` file so that preferences persist after restarts.
